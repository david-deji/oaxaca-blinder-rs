{
  "name": "meridian-browser-parity",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "COI browser byte-parity harness for the threaded WASM pay-equity engine (0014-MERIDIAN verification-benchmark D-6).",
  "scripts": {
    "pretest": "cargo run -p pay-equity-engine --example native_baseline --manifest-path ../../Cargo.toml > native-baseline.json",
    "test": "playwright test",
    "serve": "node coi-server.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.48.2"
  }
}
