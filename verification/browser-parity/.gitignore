node_modules/
test-results/
playwright-report/
.playwright-mcp/
# D3 (0014-close round-1): native decompose_inner() baseline, regenerated by
# `engine/examples/native_baseline.rs` (CI step / `npm run pretest`), not committed.
native-baseline.json
