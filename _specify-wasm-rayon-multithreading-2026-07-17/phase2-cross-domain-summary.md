# Cross-Domain Summary — Phase 2 input (orchestrator-written, 2026-07-17)

One page. Key findings each spec writer needs from OUTSIDE their own domain. Citations live in the phase1-* files.

## The project in one paragraph

Enable real Rayon thread-pool parallelism in the pay-equity-engine WASM build (sole consumer: Meridian, a client-side Vue app whose compute runs in one dedicated Web Worker, served locally by the audit-forge Flask offline bundle at /pay-equity/ on loopback:5137). Prerequisite-ordered: (1) bit-identical seeded RNG fix, (2) empirical memory profile at 50k rows, (3) threading, (4) statistical trust layer. Charter: `spec-charter.md` (13 In-Scope items, INV-01..08 inviolable). Scale: 50k employee rows; memory is the first-class constraint.

## Established facts (cross-cutting)

- **Toolchain**: threads need pinned nightly (`nightly-2024-08-02` is crate-tested) + rust-src + build-std=panic_abort,std + `-C target-feature=+atomics,+bulk-memory`; `-Zbuild-std` unstable, no stable path in 2026. wasm-bindgen-rayon 1.3.0; wasm-bindgen 0.2.106 pinned in repo. Threaded code requires wasm-bindgen `--target web` (current build uses `--target bundler` — migration mandatory regardless of strategy). Reproducibility: nightly pin is as reproducible as stable pin; empirical double-build sha256 test required; remap set likely already sufficient (~/.rustup covered).
- **Strategy decision (open — buildability gate)**: dual-artifact (stable seq blob + baseline untouched; two bindgen outputs; loader branch anyway needed for fallback) vs nightly-re-pin single artifact (one threads-capable blob, rayon seq fallback when pool never initialized; one re-baseline). Note: the `--target web` migration and the crossOriginIsolated branch are needed under BOTH strategies.
- **Memory**: 50k×10 frame ≈ 4 MB (Arrow arithmetic) — dataset is not the problem; per-rep DataFrame clones are (builder.rs:828 clones df_a/df_b per bootstrap rep). Index-vector resampling (share base frame read-only, `take(idx)`) is the memory lever AND the determinism lever. Shared memory: fixed maximum required, allocated up front, never moves, no detach-on-grow; envelope 256–512 MiB max, initial small; per-thread stack via `-C link-arg=-zstack-size` (~1 MiB); thread cap formula: N = min(hardwareConcurrency, floor((max − heap − margin)/(stack + per-rep scratch)), 8). dlmalloc stays. POLARS_MAX_THREADS=1 (one parallel layer — ours at rep level).
- **RNG/determinism (INV-02 bit-identical)**: master seed + per-rep `rand_chacha` stream (`ChaCha8Rng::set_stream(rep)` or purpose-tagged derivation); rand_chacha portable with test vectors; StdRng/SmallRng explicitly non-portable; pin crate versions; record seed+algorithm+version in outputs. Rayon indexed collect() is order-preserving; float summation must stay sequential in bootstrap_stats. Replace `sample_n_literal(...None)` (unseeded) and `thread_rng()` (quantile_decomposition.rs:215,244) with owned index resampling; fixes silent rep-discard nondeterminism too (builder.rs:827 filter_map .ok()).
- **Engine surface (5 WASM entry points)**: decompose mean path = 100-rep bootstrap (hot); quantile path = per-tau solve_qr ×2 + MM sim + bootstrap; optimize = single clarabel solve (likely skip); frontier = independent solve grid (parallel candidate — gated on clarabel thread-safety verification, currently UNVERIFIED, Phase 3 local inspection); verify_adjustments inherits decompose; check_defensibility trivial. nalgebra confirmed fine per-task on wasm.
- **Meridian wiring**: analysis.worker.js initialize() gains: crossOriginIsolated check → await initThreadPool(cap) → mode recorded and posted to UI; sequential fallback must never crash (INV-03). Top Phase 3 unknown: pool workers spawned from inside a dedicated worker (nested workers) — no published guarantee; may need a local experiment. COOP same-origin + COEP require-corp on audit-forge /pay-equity/ (insertion at webui/__init__.py:125 beside _apply_meridian_csp; loopback + no-egress = require-corp safe) + Vite server.headers for dev. Worker postMessage API stays byte-compatible (anti-goal).
- **Trust layer**: R `oaxaca` 0.1.5 (Hlavac) = same estimation procedures as Stata (Jann 2008) — one golden target; supports twofold/threefold, group.weights {0,1,0.5,Cotton,-1 Neumark,-2 pooled}, Gardeazabal-Ugidos category-invariance, bootstrap SEs (R replicates). Golden pipeline: pinned-R script + shared resample-index matrix (exact cross-implementation comparison). QR: location-scale design gives analytically known tau-varying slopes (fixes tau-insensitive tests); quantreg::rq() as reference. proptest identities: two-fold + three-fold sum, detailed-vs-aggregate, label-swap antisymmetry, scale equivariance; generators guard collinearity/tiny-groups/constant-cols. Existing repo assets to reconcile: verification/gen_parity_golden.py, tests/parity_test.rs, tests/ground_truth_verification_test.rs (added 2026-07-17).
- **Verification/CI**: parity job = byte-compare canonical JSON across native / wasm-seq / wasm-threaded(2,4) — blocking; memory-ceiling job at 50k (blocking); speedup job median-of-10, >1.5× on 4-core, scheduled/non-blocking; reproducibility double-build job. Browser jobs need a COOP/COEP-capable test server + headless Chromium (wasm-pack test COI support unverified — Phase 3). Fixture: /home/deji/Downloads/Employers_data.csv (10k rows; Gender/Salary; Age, Experience_Years, Education_Level, Department, Location, Job_Title), ×5 perturbation-replication to 50k.

## Key repo anchors (all verifiable via Read)

engine repo root: apps/hr-apps/oaxaca-blinder-rs/ — builder.rs:810-860 (bootstrap), quantile_decomposition.rs:210-260/330-345, engine/src/analysis.rs (entry points; :147 reps default; :201 API gap), engine/src/lib.rs (wasm exports), scripts/build-wasm.sh, .github/workflows/ci.yml, rust-toolchain.toml. Meridian: apps/hr-apps/pay-equity-app/frontend/src/wasm/analysis.worker.js, frontend/vite.config.js. Flask: /home/deji/telos/audit-forge/webui/__init__.py:117-159.

## Founder rulings (inviolable)

Bit-identical across modes (INV-02). Memory first-class; 50k rows (ASM-04). f64 exempt from Decimal(18,2) inside engine; Decimal at display/ledger boundaries (INV-08). Build order: determinism → memory profile → threading → validation. Engine+Meridian+audit-forge wiring all in scope; postMessage API unchanged.
